export const environment = {
  production: true,
  // Esta es la URL a la que Angular apuntará cuando esté en internet.
  // Cámbienla por la dirección real (ej. la IP del servidor o el dominio de la dependencia)
  apiUrl: 'https://www.tudominiooficial.gob.mx/api' 
};